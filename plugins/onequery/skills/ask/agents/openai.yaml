interface:
  display_name: "OneQuery CLI"
  short_description: "Inspect OneQuery-connected company data"
  default_prompt: "Use $ask to resolve org and source context, prefer --org <slug>, inspect OneQuery-connected data, validate or run a read-only query, and summarize the evidence."
