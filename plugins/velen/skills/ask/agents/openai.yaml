interface:
  display_name: "Velen CLI"
  short_description: "Inspect Velen-connected data and KG memory"
  default_prompt: "Use $ask to resolve org and source context, prefer --org <slug>, inspect Velen-connected data or insights, manage org-scoped Knowledge Graph memory when requested, run bounded read-only query validate or execute flows, and summarize the evidence."
