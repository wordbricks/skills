interface:
  display_name: "Velen CLI"
  short_description: "Inspect Velen data, memory, and CLI state"
  default_prompt: "Use $ask to resolve org and provider-qualified source context, prefer --org <slug>, inspect Velen-connected data, avoid past insight lookup unless explicitly requested, manage org-scoped Knowledge Graph or persona memory when requested, run bounded read-only query validate, query execute, or source API flows, update the CLI/agent skill when asked, review analytical drafts with `velen persona chat sophia`, require review details plus pass/fail, revise and re-review until passing before final answers, and summarize the evidence."
