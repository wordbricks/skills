interface:
  display_name: "Velen CLI"
  short_description: "Use Velen data, sources, workers, memory, and CLI"
  default_prompt: "Use $ask to resolve Velen profile, org, source, or worker context and run the smallest authorized query, source API action, connection, memory operation, or CLI management command."
