interface:
  display_name: "Velen CLI"
  short_description: "Inspect Velen data, workers, memory, and CLI state"
  default_prompt: "Use $velen-cli to inspect Velen-connected company data, resolve the right org and provider-qualified source or worker reference, run bounded read-only queries or explicitly requested worker actions, manage memory when requested, and summarize the evidence."
