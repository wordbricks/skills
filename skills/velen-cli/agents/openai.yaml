interface:
  display_name: "Velen CLI"
  short_description: "Inspect Velen data, memory, and CLI state"
  default_prompt: "Use $velen-cli to inspect Velen-connected company data, choose the right org and provider-qualified source, treat its runtime API descriptor as the capability source of truth, require explicit intent and a dry run for external writes, manage memory when requested, and summarize the evidence."
