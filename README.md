# Wordbricks Skills

Installable agent skills published by Wordbricks.

## Install

Install the `velen-cli` skill from this repo:

```bash
npx skills add wordbricks/skills --skill velen-cli
```

You can also install from a full GitHub URL:

```bash
npx skills add https://github.com/wordbricks/skills --skill velen-cli
```

## Available Skills

### `velen-cli`

Use the Velen CLI to authenticate, choose an organization, inspect connected data sources, run read-only SQL queries, and fetch saved insights by public ID.

Path: `skills/velen-cli`
